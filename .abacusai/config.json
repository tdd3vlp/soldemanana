{
  "permissions": {
    "allow": [
      "Bash(mkdir *)",
      "Bash(mkdir -p /Users/anton/Documents/habla_bot/app/api/routes && echo \"done\")",
      "Bash(touch *)",
      "Bash(cp *)",
      "Bash(cd /Users/anton/Documents/habla_bot && touch poetry.lock && ls -la | head -20)",
      "Bash(cd /Users/anton/Documents/habla_bot && touch app/core/__init__.py app/core/schemas/__init__.py app/bot/__init__.py app/i…)",
      "Bash(docker *)",
      "Bash(poetry *)",
      "Bash(python3 *)",
      "Bash(docker compose build --no-cache 2>&1 | tail -100)",
      "Bash(docker compose build bot 2>&1 || true)",
      "Bash(docker compose up -d 2>&1 || true)",
      "Bash(docker compose exec bot alembic upgrade head 2>&1 || true)",
      "Bash(ls *)",
      "Bash(docker compose restart bot 2>&1 || true)",
      "Bash(docker compose restart bot && sleep 3 && docker compose logs bot --tail 20 2>&1 || true)",
      "Bash(docker compose restart bot && sleep 3 && docker compose logs bot --tail 30 2>&1 || true)",
      "Bash(cat *)",
      "Bash(docker compose restart bot && sleep 5 && docker compose logs bot --tail 50 2>&1 || true)",
      "Bash(docker compose restart bot && sleep 5 && docker compose logs bot --tail 30 2>&1 || true)",
      "Bash(docker compose exec bot alembic upgrade head 2>&1 || true)",
      "Bash(docker compose exec bot alembic upgrade head 2>&1 || true)",
      "Bash(docker compose exec bot printenv | grep -i database 2>&1 || true)",
      "Bash(docker compose down && docker compose up -d 2>&1 || true)",
      "Bash(docker compose exec bot alembic upgrade head 2>&1 || true)",
      "Bash(docker compose down -v && docker compose up -d 2>&1 || true)",
      "Bash(sleep 5 && docker compose exec bot alembic upgrade head 2>&1 || true)",
      "Bash(docker compose down -v && docker compose up -d && sleep 5 && docker compose exec bot alembic upgrade head 2>&1 | tail -3…)",
      "Bash(docker compose stop bot && docker compose exec postgres psql -U habla -d habla_db -c \"DROP TYPE IF EXISTS language_level…)",
      "Bash(docker compose run --rm bot alembic upgrade head 2>&1 || true)",
      "Bash(docker compose build bot 2>&1 | grep -E \"(Step|Successfully)\" || true)",
      "Bash(docker compose down -v && docker compose build --no-cache bot 2>&1 | tail -20)",
      "Bash(docker compose up -d postgres redis && sleep 5 && docker compose run --rm bot alembic upgrade head 2>&1 | tail -20)",
      "Bash(docker compose up -d bot && sleep 3 && docker compose logs bot --tail 10 2>&1 || true)",
      "Bash(find *)",
      "Bash(docker compose restart bot && sleep 3 && docker compose logs bot --tail 10 2>&1 || true)",
      "Bash(docker compose restart bot && sleep 3 2>&1 || true)",
      "Bash(kill *)",
      "Bash(docker compose stop bot && docker compose exec postgres psql -U habla -d habla_db -c \"TRUNCATE users CASCADE;\" && docker…)",
      "Bash(docker compose exec bot cat /app/app/bot/middlewares/user.py | grep -A7 \"db_user = User\" 2>&1 || true)",
      "Bash(docker compose exec bot find /app -type d -name __pycache__ -exec rm -rf {} + 2>&1 || true && docker compose restart bot…)",
      "Bash(docker compose logs bot -f --tail 0 2>&1 &\nBG_PID=$!\nsleep 15\nkill $BG_PID 2>/dev/null || true)",
      "Bash(docker compose stop bot && docker compose exec postgres psql -U habla -d habla_db -c \"TRUNCATE users CASCADE;\" && sleep …)",
      "Bash(rm *)",
      "Bash(docker compose down && docker compose build --no-cache bot 2>&1 | tail -5)",
      "Bash(docker compose up -d && sleep 10 && docker compose logs bot --tail 5 2>&1 | grep -v \"level=warning\" || true)",
      "Bash(docker compose exec bot python -c \"from app.bot.middlewares.user import *; from app.core.enums import CorrectionIntensit…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 20\nkill $PID 2>/dev/null || true) | head -50)",
      "Bash(docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash(docker compose exec postgres psql -U postgres -d habla_db -c \"SELECT telegram_id, username, correction_intensity, subscr…)",
      "Bash(docker compose exec postgres psql -U habla_user -d habla_db -c \"SELECT telegram_id, username, correction_intensity, subs…)",
      "Bash(docker compose exec postgres psql -U habla -d habla_db -c \"SELECT telegram_id, username, correction_intensity, subscript…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 20\nkill $PID 2>/dev/null || true) | tail -50)",
      "Bash(docker compose exec bot cat /app/app/bot/middlewares/user.py | grep -A7 \"db_user = User\" | head -10)",
      "Bash(docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 20\nkill $PID 2>/dev/null || true) | grep -E \"(INSERT|important|…)",
      "Bash(docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 20\nkill $PID 2>/dev/null || true) | tail -40)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 25\nkill $PID 2>/dev/null || true) | grep -E \"(INSERT|SELECT.*te…)",
      "Bash(docker compose exec postgres psql -U habla -d habla_db -c \"SELECT id, telegram_id, username, first_name, correction_inte…)",
      "Bash(docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 25\nkill $PID 2>/dev/null || true) | grep -E \"(SELECT|INSERT|Err…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 30\nkill $PID 2>/dev/null || true) | tail -60)",
      "Bash(grep *)",
      "Bash(docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 30\nkill $PID 2>/dev/null || true) | tail -50)",
      "Bash((docker compose logs bot -f --tail 0 2>&1 &\nPID=$!\nsleep 35\nkill $PID 2>/dev/null || true) | grep -v \"level=warning\" | t…)",
      "Bash(docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 5 && echo \"=== Бот перезапущен ===\" 2>&1 ||…)",
      "Bash(docker compose exec -T redis redis-cli FLUSHALL 2>&1 || true)",
      "Bash(cd /Users/anton/Documents/habla_bot && docker compose exec -T bot alembic revision -m \"update_subscription_tiers_to_free…)",
      "Bash(docker compose exec -T bot ls -la /app/alembic/versions/ 2>&1 || true)",
      "Bash(docker compose cp bot:/app/alembic/versions/3ba85f6edff3_update_subscription_tiers_to_free_.py /Users/anton/Documents/ha…)",
      "Bash(docker compose stop bot && sleep 2 && docker compose build bot 2>&1 | tail -10 2>&1 || true)",
      "Bash(docker compose up -d bot && sleep 3 && docker compose exec -T bot alembic upgrade head 2>&1 | tail -20 2>&1 || true)",
      "Bash(docker compose exec -T bot alembic downgrade -1 2>&1 | tail -10 2>&1 || true)",
      "Bash(sleep 5 && docker compose exec -T bot alembic current 2>&1 | tail -5 2>&1 || true)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 5 && echo \"=== Б…)",
      "Bash(docker compose exec -T bot alembic upgrade head 2>&1 | tail -15 2>&1 || true)",
      "Bash(docker compose exec -T bot alembic downgrade 001_initial 2>&1 | tail -10 2>&1 || true)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 3 && docker comp…)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 3 && docker comp…)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -10 2>&1 || true)",
      "Bash(docker compose build bot 2>&1 | grep -A 5 \"error\\|ERROR\\|Error\" || echo \"No errors found in build\" 2>&1 || true)",
      "Bash(docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 3 && echo \"=== Бот запущен ===\" 2>&1 || tru…)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -5 && docker compose up -d bot && sleep 3 && echo \"=== Б…)",
      "Bash(docker compose stop bot && docker compose build bot 2>&1 | tail -3 && docker compose up -d bot && sleep 3 && echo \"=== Б…)"
    ]
  }
}